#include <bits/stdc++.h>
using namespace std;

class Node {
public:
    int val;
    Node* next;

    Node(val) {
        this->val = val;
        next = NULL;
    }
};
class LinkedList {
public:
    Node* head = new Node(val); 
    int length = 1;
};
void deleteMidNode(Node* head) {

}
int main() {
    
 
    return 0;
}